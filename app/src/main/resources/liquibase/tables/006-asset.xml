<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="006-create-asset-table" author="d.v.kornilovich">
        <preConditions>
            <not>
                <tableExists tableName="asset"/>
            </not>
        </preConditions>

        <createTable tableName="asset">
            <column name="id" type="bigserial">
                <constraints primaryKey="true"/>
            </column>

            <column name="user_id" type="bigint">
                <constraints foreignKeyName="fk_asset_user_id" references="user(id)"
                             nullable="false"/>
            </column>

            <column name="asset_type_id" type="bigint">
                <constraints foreignKeyName="fk_asset_asset_type_id" references="asset_type(id)"
                             nullable="false"/>
            </column>

            <column name="name" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="initial_value" type="decimal(15,2)">
                <constraints nullable="false"/>
            </column>

            <column name="currency" type="varchar(3)">
                <constraints nullable="false"/>
            </column>

            <column name="acquired_at" type="timestamp">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="timestamp"
                    defaultValueComputed="CURRENT_TIMESTAMP"/>
            <column name="updated_at" type="timestamp"/>
        </createTable>
    </changeSet>

</databaseChangeLog>